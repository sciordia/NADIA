% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_rank_mds1}
\alias{nm_rank_mds1}
\title{Rank normalization methods by MDS1 variance explained}
\usage{
nm_rank_mds1(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  verbose = TRUE
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{data.frame} with columns \code{Method}, \code{MDS1_VarPct}, \code{Rank},
ordered by \code{MDS1_VarPct} ascending (lower = better; rank 1 = best).
}
\description{
For each assay in \code{se}, computes the percentage of variance captured by the
first MDS dimension (using only positive eigenvalues from classical MDS) and
returns a ranking ordered by ascending MDS1 variance (lower = better).
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                            verbose = FALSE)

nm_rank_mds1(se, verbose = FALSE)

}
