% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_compute_metrics}
\alias{nm_compute_metrics}
\title{Compute quantitative group-separation metrics per normalization method}
\usage{
nm_compute_metrics(se, assay_names = NULL, condition_col = "Condition", ...)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{...}{Additional arguments (unused).}
}
\value{
A \code{data.frame} with one row per method and columns:
\code{Method}, \code{PC1_VarPct}, \code{PC1_F_ratio}, \code{PERMANOVA_R2}, \code{PERMANOVA_pval},
\code{Silhouette_mean}, \code{MDS_GOF}, \code{MDS_CophCor}, \code{Spectral_Entropy},
\code{CumVar_PC2}, \code{Hopkins}, \code{Condition_Number}.
}
\description{
Iterates over assays in a SummarizedExperiment and computes ten metrics
that quantify how well the normalization separates sample groups.
}
\details{
\itemize{
\item \strong{PC1_VarPct}: \% variance explained by PC1 (higher = more structure).
\item \strong{PC1_F_ratio}: ANOVA F-ratio on PC1 scores (higher = better separation).
\item \strong{PERMANOVA_R2}: Proportion of variance explained by grouping (requires
vegan; NA if not installed).
\item \strong{Silhouette_mean}: Mean silhouette width (requires cluster; NA if not
installed).
\item \strong{MDS_GOF}: Goodness-of-fit of 2D MDS projection. Note: in benchmarking
context, \emph{lower} values tend to indicate better normalization (a good method
preserves multi-dimensional biological variability that 2D cannot capture).
\item \strong{MDS_CophCor}: Cophenetic correlation between original and MDS distances.
Same caveat as MDS_GOF: \emph{lower} values tend to correlate with better
benchmarking performance.
\item \strong{Spectral_Entropy}: Normalized Shannon entropy of PCA eigenvalues (0-1).
Values near 0 = variance concentrated in few PCs; near 1 = uniform spread.
\item \strong{CumVar_PC2}: Cumulative \% variance explained by PC1 + PC2 (0-100).
\item \strong{Hopkins}: Hopkins statistic for clustering tendency (0-1). Values > 0.5
suggest non-random cluster structure.
\item \strong{Condition_Number}: Ratio of largest to smallest PCA eigenvalue (>= 1).
High values indicate multicollinearity or numerical instability.
}
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                            verbose = FALSE)

metrics_df <- nm_compute_metrics(se)
metrics_df[, c("Method", "PC1_VarPct", "PC1_F_ratio", "Silhouette_mean")]

}
