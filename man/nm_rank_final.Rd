% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_rank_final}
\alias{nm_rank_final}
\title{Compute a combined final ranking across PCV, PMAD, PEV, Correlation and
group-separation (PC1 F-ratio)}
\usage{
nm_rank_final(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  cor_method = "pearson",
  verbose = TRUE
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{cor_method}{Correlation method forwarded to \code{nm_rank_cor()}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{data.frame} with columns: \code{Method}, \code{Rank_PCV}, \code{Rank_PMAD},
\code{Rank_PEV}, \code{Rank_Cor}, \code{Rank_Sep}, \code{Rank_Final}, ordered by \code{Rank_Final}
ascending (best first).
}
\description{
For each method, the final rank is the mean of five individual ranks:
four intragroup-precision metrics (pcv, pmad, pev, cor) plus a
group-separation metric (\code{Rank_Sep}, the ANOVA F-ratio on PC1 scores,
higher = better separation). Lower final rank = better overall
normalization quality.
}
\details{
Note: earlier versions averaged \code{Rank_PC1} (PC1 variance \%, higher = better)
and \code{Rank_MDS1} (MDS1 variance \%, lower = better). Both capture the same
dominant axis of variation but with opposite directions, so they partially
cancelled within the mean. They are replaced here by a single, unambiguous
group-separation rank based on the PC1 F-ratio. The standalone
\code{nm_rank_pc1()} / \code{nm_rank_mds1()} functions are unchanged for individual
inspection.
}
