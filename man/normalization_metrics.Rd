% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{normalization_metrics}
\alias{normalization_metrics}
\title{Generate quality metric plots for normalization comparison}
\usage{
normalization_metrics(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  plots = "all",
  cor_method = "pearson",
  pca_scales = c("free", "fixed", "both"),
  mds_scales = c("free", "fixed", "both"),
  methods = NULL,
  method_args = list(),
  base_assay = "log2",
  seed = 42L,
  verbose = TRUE,
  output_dir = NULL,
  export_plots = TRUE,
  export_tables = TRUE,
  plot_width = 12,
  plot_height = 8,
  plot_dpi = 150
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include.
NULL (default) = all assays.}

\item{condition_col}{Column name in \code{colData(se)} with condition labels.
Default \code{"Condition"}.}

\item{plots}{Character vector of plot names to generate, or \code{"all"} (default).
Valid names: \code{"boxplot"}, \code{"density"}, \code{"pcv"}, \code{"pmad"}, \code{"pev"},
\code{"pca"}, \code{"correlation"}, \code{"mds"}, \code{"scatter"}, \code{"qq"}, \code{"metrics"},
\code{"pc1_ranking"}, \code{"mds1_ranking"}, \code{"final_ranking"}.
When \code{pca_scales = "both"}, \code{"pca"} expands to \code{"pca_free"} + \code{"pca_fixed"}.
When \code{mds_scales = "both"}, \code{"mds"} expands to \code{"mds_free"} + \code{"mds_fixed"}.}

\item{cor_method}{Correlation method for \code{nm_plot_correlation()}.
Default \code{"pearson"}.}

\item{pca_scales}{Facet scaling for PCA plot: \code{"free"} (default),
\code{"fixed"}, or \code{"both"} to generate and export both variants
(\code{pca_free} and \code{pca_fixed} in the returned list).}

\item{mds_scales}{Facet scaling for MDS plot: \code{"free"} (default),
\code{"fixed"}, or \code{"both"} to generate and export both variants
(\code{mds_free} and \code{mds_fixed} in the returned list).}

\item{methods}{Character vector of normalization method names to
auto-benchmark, \code{"all"} for all 14 methods, or NULL (default) to skip
auto-normalization and use existing assays.}

\item{method_args}{Named list of per-method arguments forwarded to
\code{nm_run_normalizations()}. E.g.
\code{list(cycloess = list(method = "fast", span = 0.8))}.}

\item{base_assay}{Name of the baseline assay to normalize from when using
auto-normalization. Default \code{"log2"}.}

\item{seed}{Integer. Seed passed to \code{nm_compute_metrics()} for the two
metrics that use randomness, the Hopkins statistic and the PERMANOVA
p-value. Default 42.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}

\item{output_dir}{Character. Path to export directory. If \code{NULL} (default),
no files are exported. When set, tables (TSV) and/or plots (PNG) are
saved to this directory (created if needed).}

\item{export_plots}{Logical. Export plots as PNG when \code{output_dir} is set.
Default \code{TRUE}.}

\item{export_tables}{Logical. Export tables as TSV when \code{output_dir} is set.
Default \code{TRUE}.}

\item{plot_width}{Numeric. Width in inches for exported plots. Default \code{12}.}

\item{plot_height}{Numeric. Height in inches for exported plots. Default \code{8}.}

\item{plot_dpi}{Numeric. Resolution for exported plots. Default \code{150}.}
}
\value{
Named list of ggplot objects (or NULL for failed plots), plus
data.frames always computed regardless of \code{plots} selection:
\code{metrics_table} (from \code{nm_compute_metrics()}), \code{pc1_rank}, \code{mds1_rank},
\code{pcv_rank}, \code{pmad_rank}, \code{pev_rank}, \code{cor_rank}, and \code{final_rank}
(combined ranking as mean of the six individual ranks).
}
\description{
Calls all (or selected) \verb{nm_plot_*()} functions on a SummarizedExperiment
produced by \code{import_norm_matrices()}. Each plot is wrapped in \code{tryCatch()}
so that a failure in one plot does not abort the entire run.
}
\details{
When \code{methods} is provided, the function first runs
\code{nm_run_normalizations()} to automatically apply the requested normalization
methods from the \code{base_assay}, then computes metrics and generates plots on
the resulting multi-assay SE.
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)

# Auto-benchmark: normalize from the "log2" baseline, then score the methods
res <- normalization_metrics(se, methods = c("cycloess", "quantile", "MAD"),
                             plots = c("pcv", "final_ranking"), verbose = FALSE)
res$final_rank
res$pcv

}
