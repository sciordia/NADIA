% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_plot_pca}
\alias{nm_plot_pca}
\title{PCA scatter plot per method}
\usage{
nm_plot_pca(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  pca_scales = c("free", "fixed"),
  ...
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{pca_scales}{Facet scaling: \code{"free"} (default) allows independent axes
per method; \code{"fixed"} uses shared axes to compare separation magnitude.}

\item{...}{Additional arguments (unused).}
}
\value{
ggplot object.
}
\description{
PC1 vs PC2 scatter, colored by condition, faceted by method.
Percentage of variance explained shown on each axis.
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                            verbose = FALSE)

nm_plot_pca(se, pca_scales = "fixed")

}
