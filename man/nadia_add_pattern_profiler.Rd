% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database.R
\name{nadia_add_pattern_profiler}
\alias{nadia_add_pattern_profiler}
\title{Add a Pattern Profiler run to an existing \code{.nadia} file}
\usage{
nadia_add_pattern_profiler(
  file,
  pattern_profiler,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to an existing \code{.nadia} file.}

\item{pattern_profiler}{Object from \code{\link[=pattern_profiler_analysis]{pattern_profiler_analysis()}}.}

\item{overwrite}{Replace a clustering already in the file.}

\item{verbose}{Print progress.}
}
\value{
The path, invisibly.
}
\description{
The clustering is an optional extra step, so it is written separately: this
adds it to a file that already holds the analysis, and refuses to create one
from scratch.
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    res <- process_proteomics(nadia_dia, verbose = FALSE)
    f <- file.path(tempdir(), "pp.nadia")
    write_nadia(f, nadia_dia, res, verbose = FALSE)

    # pp <- pattern_profiler_analysis(res$se_proc, res$DEPs_results)
    # nadia_add_pattern_profiler(f, pp)

    unlink(f)
}
}
\seealso{
\code{\link[=write_nadia]{write_nadia()}}
}
