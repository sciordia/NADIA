% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{nadia_deps_with_clusters}
\alias{nadia_deps_with_clusters}
\title{Differential abundance with clusters, from a \code{.nadia} file}
\usage{
nadia_deps_with_clusters(
  db,
  assignment = c("primary", "all"),
  min_membership = NULL,
  comparison = NULL,
  significant_only = FALSE
)
}
\arguments{
\item{db}{A \code{nadia_db} from \code{\link[=read_nadia]{read_nadia()}}, or the path to a \code{.nadia} file.}

\item{assignment}{\code{"primary"} (default) for one row per protein and
comparison, carrying the cluster of highest membership; \code{"all"} for one row
per protein, comparison and cluster.}

\item{min_membership}{Optional extra membership threshold. It can only be
stricter than the one the analysis ran with.}

\item{comparison}{Optional character vector of comparisons to keep.}

\item{significant_only}{Keep only the rows classified \code{Up} or \code{Down}.}
}
\value{
A data frame with the columns of \code{DEPs_results} followed by
\code{Cluster}, \code{Membership} and \code{ClusterRank}, or \code{NULL} if the file holds no
differential-abundance results. A file with results but no clustering
returns the same shape with the three cluster columns \code{NA}.
}
\description{
The file's own answer to \code{\link[=deps_with_clusters]{deps_with_clusters()}}. The join lives in the file
as the view \code{v_deps_pattern_profiler}, so this reads a table rather than
building one, and a client that is not R gets the same table from the same
SQL. The filters are the ones \code{\link[=deps_with_clusters]{deps_with_clusters()}} applies, and the two
functions return identical results for the same arguments.
}
\details{
The table is read on demand rather than by \code{\link[=read_nadia]{read_nadia()}}, because it is as
long as the number of proteins times comparisons times clusters and there is
no reason to carry it around unasked.
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    res <- process_proteomics(nadia_dia, verbose = FALSE)

    f <- file.path(tempdir(), "functional.nadia")
    write_nadia(f, nadia_dia, res, verbose = FALSE)

    head(nadia_deps_with_clusters(f))

    unlink(f)
}
}
\seealso{
\code{\link[=deps_with_clusters]{deps_with_clusters()}}, \code{\link[=nadia_add_pattern_profiler]{nadia_add_pattern_profiler()}}
}
