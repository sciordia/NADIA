% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization.R
\name{normalize_proteomics}
\alias{normalize_proteomics}
\title{Normalize proteomics data}
\usage{
normalize_proteomics(
  data,
  metadata = NULL,
  min_reps = NULL,
  min_groups = 1,
  norm_method = "cycloess",
  cyclic_loess_method = c("fast", "pairs"),
  cyclic_loess_iterations = 3,
  cyclic_loess_span = 0.7,
  verbose = TRUE
)
}
\arguments{
\item{data}{Either a \code{proteomics_data} object -- the output of
\code{\link[=preprocess_spectronaut]{preprocess_spectronaut()}}, \code{\link[=preprocess_tmt]{preprocess_tmt()}} or \code{\link[=preprocess_lfq]{preprocess_lfq()}} -- in
which case \code{metadata} is derived from it and must be left \code{NULL}, or a data
frame with ProteinGroups, GeneNames, UniqPepts and the intensity columns.}

\item{metadata}{Data frame with Column, Condition, Replicate. Only needed when
\code{data} is a data frame.}

\item{min_reps}{Minimum replicates for filtering. NULL = auto: floor(min_group_size / 2)}

\item{min_groups}{Minimum groups meeting min_reps (default: 1)}

\item{norm_method}{Normalization method (default: "cycloess"). One of:
\itemize{
\item Group A (input: raw intensities): "log2Norm", "GlobalMedian",
"GlobalMean", "eqmedians", "vsn"
\item Group B (input: log2 assay): "log2" (no extra normalization),
"quantile", "Rlr", "MAD", "cycloess",
"medianNorm", "meanNorm", "quantile.robust"
}}

\item{cyclic_loess_method}{Cyclic Loess method: "fast" or "pairs" (default: "fast")}

\item{cyclic_loess_iterations}{Number of iterations for Cyclic Loess (default: 3)}

\item{cyclic_loess_span}{Span parameter for Cyclic Loess (default: 0.7)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
List with:
\itemize{
\item se: SummarizedExperiment with assays raw, log2, and <norm_method>
(assays raw + log2 only when norm_method = "log2")
\item filter_summary: Filtering summary
\item na_overview: NA statistics
}
}
\description{
Complete normalization pipeline: zero-to-NA conversion, protein filtering
by group presence, SummarizedExperiment creation, and normalization with
the selected method. Output is always in log2 scale.
}
\examples{
\dontrun{
norm_result <- normalize_proteomics(
  data = protein_data,
  metadata = metadata,
  norm_method = "cycloess",
  cyclic_loess_method = "fast"
)
norm_result2 <- normalize_proteomics(
  data = protein_data,
  metadata = metadata,
  norm_method = "GlobalMedian"
)
}

}
