% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database.R
\name{write_nadia}
\alias{write_nadia}
\title{Write a complete analysis to a single \code{.nadia} file}
\usage{
write_nadia(
  file,
  preprocessing,
  result = NULL,
  pattern_profiler = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to write to. The \code{.nadia} extension is a
convention; the file is a DuckDB database whatever it is called.}

\item{preprocessing}{Object from \code{preprocess_spectronaut()},
\code{preprocess_tmt()} or \code{preprocess_lfq()}. Required: it is the base of the
file.}

\item{result}{Object from \code{\link[=process_proteomics]{process_proteomics()}}, or \code{NULL} to
store only the preprocessing.}

\item{pattern_profiler}{Object from \code{\link[=pattern_profiler_analysis]{pattern_profiler_analysis()}}, or \code{NULL}.
Can also be added later with \code{\link[=nadia_add_pattern_profiler]{nadia_add_pattern_profiler()}}.}

\item{overwrite}{Replace \code{file} if it already exists.}

\item{verbose}{Print progress.}
}
\value{
The path, invisibly.
}
\description{
Stores the preprocessing tables, the processing results and, optionally, the
Pattern Profiler output in one DuckDB database, together with the parameters
and the provenance of the run. Nothing is duplicated: everything that can be
derived is left out and rebuilt on reading, through SQL views stored in the
file itself.
}
\details{
Use \code{\link[=read_nadia]{read_nadia()}} to read it back, and \code{\link[=nadia_result]{nadia_result()}} to get an object the
rest of the package can plot directly.
}
\section{What goes in}{

Twelve tables. \code{samples} and \code{proteins} are the dimensions;
\code{protein_sample_metrics}, \code{intensities}, \code{imputed_values} and \code{de_results}
the facts; \code{pp_membership} and \code{pp_profile} the optional clustering; and
\code{nadia_meta}, \code{nadia_packages}, \code{nadia_source_files}, \code{nadia_calls},
\code{parameters} and \code{nadia_schema} the provenance.

Three things are deliberately \emph{not} stored, because they are exactly
derivable: the \code{log2} assay (\code{log2} of the raw one), the \code{PCA_Input} table
(the imputed assay joined to the DE results) and the observed cells of the
imputed assay (identical to the normalized ones). The last of these is
checked rather than assumed, since a model-based method such as \code{limpa} can
revise observed values; when it does, the full assay is stored instead.
}

\section{Where not to put it}{

Not inside a synchronised folder such as iCloud Drive, Dropbox or OneDrive
while you are working. DuckDB keeps the file open with locks and partial
writes, and a synchronising client can corrupt it. Write it locally and copy
it afterwards.
}

\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    res <- process_proteomics(nadia_dia, verbose = FALSE)

    f <- file.path(tempdir(), "example.nadia")
    write_nadia(f, nadia_dia, res, verbose = FALSE)

    db <- read_nadia(f)
    db

    unlink(f)
}
}
\seealso{
\code{\link[=read_nadia]{read_nadia()}}, \code{\link[=nadia_result]{nadia_result()}}, \code{\link[=nadia_add_pattern_profiler]{nadia_add_pattern_profiler()}}
}
