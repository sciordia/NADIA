% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Processing.R
\name{process_proteomics}
\alias{process_proteomics}
\title{Process proteomics data}
\usage{
process_proteomics(
  preprocessing,
  export_dir = NULL,
  min_reps_filter = NULL,
  min_groups_filter = 1,
  norm_method = "cycloess",
  cyclic_loess_method = "fast",
  cyclic_loess_iterations = 3,
  cyclic_loess_span = 0.7,
  batch_correct = FALSE,
  batch_column = "Batch",
  batch_algorithm = "ComBat",
  batch_ComBat_mode = 1,
  batch_covariates = NULL,
  batch_qualitycontrol = FALSE,
  imp_method = "combo",
  mar_method = "Impseqrob",
  mnar_method = "min",
  prop_na_mnar = 0.51,
  prop_present_mar = 0.5,
  min_present_mar = 1,
  require_n_conditions = 1,
  max_na_prop = NULL,
  method_args = list(),
  with_value = NA_real_,
  comparisons = NULL,
  control = NULL,
  logFC_threshold = 0,
  alpha = 0.05,
  eBayes_trend = NULL,
  eBayes_robust = NULL,
  de_method = "limma",
  covariate_df = NULL,
  covariate_column = NULL,
  bio_replicate_column = NULL,
  export_normalized = TRUE,
  export_imputed = TRUE,
  export_format = "tsv",
  export_volcano = TRUE,
  export_boxplot = TRUE,
  export_pca = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{preprocessing}{A \code{proteomics_data} object, as returned by
\code{\link[=preprocess_spectronaut]{preprocess_spectronaut()}}, \code{\link[=preprocess_diann]{preprocess_diann()}}, \code{\link[=preprocess_tmt]{preprocess_tmt()}} or
\code{\link[=preprocess_lfq]{preprocess_lfq()}}.}

\item{export_dir}{Output directory for exported files. Defaults to \code{NULL},
which writes nothing to disk; pass a path to enable the exports controlled
by \code{export_normalized}, \code{export_imputed}, \code{export_volcano},
\code{export_boxplot} and \code{export_pca}.}

\item{min_reps_filter}{Minimum replicates for filtering. If NULL, auto-computed}

\item{min_groups_filter}{Minimum groups for filtering (default: 1)}

\item{norm_method}{Normalization method passed to normalize_proteomics()
(default: "cycloess"). See normalize_proteomics() for all 13 options.}

\item{cyclic_loess_method}{Cyclic Loess method: "fast" or "pairs" (default: "fast")}

\item{cyclic_loess_iterations}{Number of iterations for Cyclic Loess (default: 3)}

\item{cyclic_loess_span}{Span parameter for Cyclic Loess (default: 0.7)}

\item{batch_correct}{Logical. Apply BERT batch correction after
normalization (default: FALSE). Requires covariate_df with batch_column.}

\item{batch_column}{Column in covariate_df containing batch assignments
(default: "Batch"). Must have >= 2 unique values.}

\item{batch_algorithm}{Batch correction algorithm: "ComBat" (default), "limma", or "ref"}

\item{batch_ComBat_mode}{Integer 1-4 for ComBat parametric/mean-only settings (default: 1)}

\item{batch_covariates}{Character vector of colData column names to protect
during BERT batch correction (default: NULL). Categorical columns are
encoded as indicator variables automatically; numeric columns are passed
through unchanged and therefore act as continuous covariates.}

\item{batch_qualitycontrol}{Logical. Compute ASW quality metrics (default: FALSE)}

\item{imp_method}{Imputation method (default: "combo"). See impute_proteomics() for all options.}

\item{mar_method}{MAR method for combo mode (default: "Impseqrob")}

\item{mnar_method}{MNAR method for combo mode (default: "min")}

\item{prop_na_mnar}{NA proportion for MNAR classification (default: 0.51)}

\item{prop_present_mar}{Present proportion for MAR (default: 0.5)}

\item{min_present_mar}{Minimum present values for MAR (default: 1)}

\item{require_n_conditions}{Required conditions with presence (default: 1)}

\item{max_na_prop}{Maximum NA proportion above which a protein is removed
before imputation. \code{NULL} (the default) disables the filter.}

\item{method_args}{Named list of per-method argument lists}

\item{with_value}{Constant value for imp_method="with"}

\item{comparisons}{Comparisons for DE. If NULL, generates all pairwise}

\item{control}{Control condition. If NULL, compares all}

\item{logFC_threshold}{LogFC threshold for significance (default: 0)}

\item{alpha}{Adjusted p-value threshold (default: 0.05)}

\item{eBayes_trend}{Use trend estimation in eBayes. If NULL (default), it is
resolved from de_method: TRUE for "limma", FALSE for "limpa".}

\item{eBayes_robust}{Use robust estimation in eBayes. If NULL (default), it is
resolved from de_method: TRUE for "limma", FALSE for "limpa".}

\item{de_method}{DE method: "limma" (default) or "limpa" (probabilistic, requires imp_method="limpa")}

\item{covariate_df}{Data frame with Column + covariate column(s) for paired/blocked design (default: NULL)}

\item{covariate_column}{Name(s) of the covariate column(s) for the DE model.
Single string (e.g., "Subject") or character vector (e.g., c("Subject", "Batch")). Default: NULL}

\item{bio_replicate_column}{Column name in covariate_df identifying biological replicates
(e.g., "Patient", "Subject"). Used with limma::duplicateCorrelation() to account for
technical replicates or paired designs via random effect blocking. Default: NULL}

\item{export_normalized}{Export normalized matrix (default: TRUE)}

\item{export_imputed}{Export imputed matrix (default: TRUE)}

\item{export_format}{Export format: "tsv", "parquet", or "both" (default: "tsv")}

\item{export_volcano}{Export VolcanoPlot_Input (default: TRUE)}

\item{export_boxplot}{Export BoxPlot_Input (default: TRUE)}

\item{export_pca}{Export PCA_Input (default: TRUE)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
List of class proteomics_result with:
\itemize{
\item se_proc: Processed SummarizedExperiment with all assays
\item DEPs_results: Data frame with differential expression results
\item BoxPlot_Input: Long-format data frame with one row per protein,
sample and assay (columns Column, Assay, Intensity, Condition,
Replicate, Protein.IDs). Feed it to \code{\link[=boxplot_highchart_list]{boxplot_highchart_list()}}.
\item PCA_Input: Long-format data frame with the imputed intensities plus
one adjP_* column per comparison and a sig_any flag. Feed it to
\code{\link[=pca_highchart_list]{pca_highchart_list()}}, \code{\link[=proteomics_heatmap]{proteomics_heatmap()}} or
\code{\link[=proteomics_heatmap_list]{proteomics_heatmap_list()}}.
\item comparisons: Comparisons performed
\item parameters: Parameters used
}

\code{BoxPlot_Input} and \code{PCA_Input} are always returned, whether or not
\code{export_dir} is given: the plotting layer needs them, and requiring a round
trip through disk to obtain them would be gratuitous.
}
\description{
Complete processing pipeline: filtering, normalization, imputation
and differential expression analysis. Coordinates sub-modules
Normalization.R, Imputation.R, and DEAnalysis.R.
}
\details{
The input is a \code{proteomics_data} object, which every preprocessing function
returns with the same three elements, so the pipeline is indifferent to the
acquisition and the search engine the data came from.
}
\examples{
data(nadia_dia)

# Normalization, imputation and differential expression in one call
res <- process_proteomics(nadia_dia, verbose = FALSE)
res

SummarizedExperiment::assayNames(res$se_proc)
table(res$DEPs_results$Comparison, res$DEPs_results$Change)

# The plotting layer is fed straight from the result, no files involved
names(res$BoxPlot_Input)
names(res$PCA_Input)

# Another combination: quantile normalization and a single imputation method
res2 <- process_proteomics(nadia_dia, norm_method = "quantile",
                           imp_method = "QRILC", verbose = FALSE)
SummarizedExperiment::assayNames(res2$se_proc)

}
