% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill.R
\name{nadia_skill_path}
\alias{nadia_skill_path}
\title{Locate the NADIA agent skill}
\usage{
nadia_skill_path(...)
}
\arguments{
\item{...}{Character path segments below the skill directory, passed on to
\code{\link[base:system.file]{base::system.file()}}. With no arguments the directory itself is returned.}
}
\value{
A single character string: the absolute path, or \code{""} when the skill
cannot be found, which is what \code{system.file()} returns for a missing file.
}
\description{
Returns the directory holding the agent skill that ships with NADIA. The
skill documents the analysis pipeline for a coding assistant: which function
to call at each stage, how to choose between the normalisation and imputation
methods, what to verify afterwards, and the mistakes the package's defaults
invite. It is plain Markdown, so any assistant can read it, and it follows
the layout Claude expects for a skill: a \code{SKILL.md} entry point that points
at the files under \verb{reference/} and \verb{scripts/}.
}
\details{
To use it with Claude Code, copy the directory into the skills folder of a
project (\verb{.claude/skills/nadia/}) or of the user (\verb{~/.claude/skills/nadia/}).
The examples below print the command that does it.
}
\examples{
# Where the skill lives
nadia_skill_path()

# Its entry point, and the files it refers to
nadia_skill_path("SKILL.md")
list.files(nadia_skill_path(), recursive = TRUE)

# Install it for Claude Code, for the current user
cat(sprintf(
    "cp -R '\%s' ~/.claude/skills/nadia",
    nadia_skill_path()
))
}
\seealso{
\code{vignette("NADIA")} for the same pipeline written for a person.
}
