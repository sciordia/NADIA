% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{im_compute_metrics}
\alias{im_compute_metrics}
\title{Compute imputation quality metrics (NRMSE, SOR, PSS, ACC_OI)}
\usage{
im_compute_metrics(
  se,
  assay_name = NULL,
  methods = .IM_BENCH_METHODS,
  combo_methods = list(),
  condition_col = "Condition",
  na_prop = 0.2,
  seed = 42L,
  pattern = "random",
  method_args = list(),
  with_value = NA_real_,
  verbose = TRUE
)
}
\arguments{
\item{se}{SummarizedExperiment (from \code{import_imp_matrices()} or pipeline)}

\item{assay_name}{Character. Name of the assay to use as starting point
(typically the normalized assay, pre-imputation, with real NAs).}

\item{methods}{Character vector of individual imputation methods to benchmark.
Default: \code{.IM_BENCH_METHODS} (14 methods). Use \code{character(0)} or \code{NULL}
to skip individual methods.}

\item{combo_methods}{Named list of combo/softHybrid configurations. Each
element is a list with: \code{mode} ("combo" or "softHybrid", default "combo"),
\code{mar_method}, \code{mnar_method}. The list name is used as the method label
in results. Default: empty list (no combo methods).}

\item{condition_col}{Character. Column in colData with condition labels,
required for combo methods. Default \code{"Condition"}.}

\item{na_prop}{Numeric (0-1). Proportion of artificial NAs. Default 0.20.
Only used when \code{pattern = "random"}. Ignored when \code{pattern = "from_data"}
(NA proportion is derived from the data, replicating NAguideR).}

\item{seed}{Integer. Random seed. Default 42.}

\item{pattern}{Character. NA introduction pattern: \code{"random"} for uniform
random NAs, or \code{"from_data"} to replicate NAguideR's simulation strategy
(row ratio + per-column distribution from the actual data).}

\item{method_args}{Named list of per-method argument lists.}

\item{with_value}{Constant value for method \code{"with"}.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
data.frame with columns: Method, NRMSE, SOR, PSS, ACC_OI,
NRMSE_Rank, SOR_Rank, PSS_Rank, ACC_OI_Rank, Rank_Mean.
Ordered by Rank_Mean (best first).
}
\description{
Extracts complete-case rows from the assay, introduces artificial NAs,
re-imputes with each method, and computes four quality metrics plus
a combined ranking.
}
\examples{
\dontrun{
# Individual methods only
metrics <- im_compute_metrics(se, assay_name = "cycloess",
                               methods = c("knn", "min", "zero"))

# Combo + individual
metrics <- im_compute_metrics(se, assay_name = "cycloess",
  methods = c("knn", "min"),
  combo_methods = list(
    "Impseq+min" = list(mar_method = "Impseq", mnar_method = "min")
  ))
}
}
