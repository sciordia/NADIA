% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{read_nadia}
\alias{read_nadia}
\title{Read a \code{.nadia} file}
\usage{
read_nadia(file)
}
\arguments{
\item{file}{Path to a \code{.nadia} file.}
}
\value{
An object of class \code{nadia_db}: a list with \code{file}, \code{meta},
\code{parameters}, \code{calls}, \code{packages}, \code{source_files}, the canonical tables
(\code{metadata}, \code{protein_id}, \code{protein_quant} and, when present,
\code{DEPs_results}, \code{BoxPlot_Input}, \code{PCA_Input}, \code{pattern_profiler}), the
\code{matrices} and the \code{imputed_values} mask.
}
\description{
Loads the whole analysis: the preprocessing tables, the processing results,
the intensity matrices, the parameters and the provenance. The canonical
tables come out of the SQL views stored in the file, with their R classes
and column order restored.
}
\details{
To get objects the rest of the package can use directly, pass the result to
\code{\link[=nadia_preprocessing]{nadia_preprocessing()}} or \code{\link[=nadia_result]{nadia_result()}}.
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    res <- process_proteomics(nadia_dia, verbose = FALSE)
    f <- file.path(tempdir(), "read.nadia")
    write_nadia(f, nadia_dia, res, verbose = FALSE)

    db <- read_nadia(f)
    db
    identical(db$protein_quant, nadia_dia$protein_quant)

    unlink(f)
}
}
\seealso{
\code{\link[=write_nadia]{write_nadia()}}, \code{\link[=nadia_result]{nadia_result()}}, \code{\link[=nadia_preprocessing]{nadia_preprocessing()}}
}
