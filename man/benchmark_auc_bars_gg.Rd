% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Benchmarking_Single.R
\name{benchmark_auc_bars_gg}
\alias{benchmark_auc_bars_gg}
\title{AUC Bar Chart (ggplot2)}
\usage{
benchmark_auc_bars_gg(
  metrics_table,
  title = "AUC by Comparison",
  bar_width = 0.7
)
}
\arguments{
\item{metrics_table}{Data frame from compute_benchmark_metrics()}

\item{title}{Plot title}

\item{bar_width}{Bar width (default: 0.7)}
}
\value{
ggplot2 object
}
\description{
Horizontal bar chart of AUC per comparison, colored by value,
with a reference line at 0.5.
}
\examples{
data(nadia_dia)
de <- process_proteomics(nadia_dia, verbose = FALSE)$DEPs_results
sp <- utils::read.delim(system.file("extdata", "nadia_dia_spikein.tsv.gz",
                                    package = "NADIA"))
de$Species <- sp$PG.OrganismId[match(de$Protein.IDs, sp$PG.ProteinGroups)]
ev <- data.frame(Comparison = rep(c("B-A", "D-A"), each = 2),
                 Species = c("ECOLI", "YEAST"),
                 expected_logFC = c(1, -0.58, 2, -3.3))
if (requireNamespace("pROC", quietly = TRUE)) {
  metrics <- compute_benchmark_metrics(de, ev)
  benchmark_auc_bars_gg(metrics)
}
}
