% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{import_imp_matrices}
\alias{import_imp_matrices}
\title{Import imputed matrices into a SummarizedExperiment}
\usage{
import_imp_matrices(
  tsv_dir,
  metadata_path,
  pattern = "matrix_log2_.*\\\\.tsv$",
  method_names = NULL,
  condition_col = "Condition",
  sample_col = "Column"
)
}
\arguments{
\item{tsv_dir}{Character. Directory containing the TSV files.}

\item{metadata_path}{Character. Path to a TSV or CSV file with at minimum
columns \code{sample_col} and \code{condition_col}.}

\item{pattern}{Character. Regex to filter files in \code{tsv_dir}.
Default: \code{"matrix_log2_.*\\\\.tsv$"}.}

\item{method_names}{Character vector. Assay names to assign (in order).
NULL (default) = extracted from file names between \code{matrix_log2_} and \code{.tsv}.}

\item{condition_col}{Character. Column in metadata with condition/group labels.}

\item{sample_col}{Character. Column in metadata with sample names.}
}
\value{
A SummarizedExperiment with one assay per TSV, colData from metadata,
and rowData containing \code{Protein.IDs}.
}
\description{
Reads all TSV files matching a pattern from a directory, one assay per file.
Each TSV must have column 1 as protein identifier and remaining columns as
sample names matching the \code{sample_col} in the metadata file.
}
\examples{
\dontrun{
se_imp <- import_imp_matrices(
  tsv_dir       = "./results",
  metadata_path = "./data/metadata.tsv"
)
SummarizedExperiment::assayNames(se_imp)
}
}
