% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{nadia_preprocessing}
\alias{nadia_preprocessing}
\title{Rebuild the preprocessing object from a \code{.nadia} file}
\usage{
nadia_preprocessing(db)
}
\arguments{
\item{db}{A \code{nadia_db} from \code{\link[=read_nadia]{read_nadia()}}.}
}
\value{
A \code{proteomics_data} object with \code{metadata}, \code{protein_id} and
\code{protein_quant}.
}
\description{
Returns an object indistinguishable from what \code{preprocess_spectronaut()},
\code{preprocess_tmt()} or \code{preprocess_lfq()} produced, so it can be fed straight
to \code{\link[=process_proteomics]{process_proteomics()}} or to the table functions.
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    f <- file.path(tempdir(), "pre.nadia")
    write_nadia(f, nadia_dia, verbose = FALSE)

    pre <- nadia_preprocessing(read_nadia(f))
    identical(pre$protein_id, nadia_dia$protein_id)

    unlink(f)
}
}
\seealso{
\code{\link[=read_nadia]{read_nadia()}}, \code{\link[=nadia_result]{nadia_result()}}
}
