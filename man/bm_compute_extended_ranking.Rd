% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Benchmarking_Multiple.R
\name{bm_compute_extended_ranking}
\alias{bm_compute_extended_ranking}
\title{Compute extended ranking across 11 classification + OpDEA metrics}
\usage{
bm_compute_extended_ranking(
  opdea_combined,
  bench_metrics_combined,
  metrics = .BM_EXTENDED_METRICS
)
}
\arguments{
\item{opdea_combined}{data.frame from import_opdea_results()}

\item{bench_metrics_combined}{data.frame from import_benchmark_metrics()}

\item{metrics}{Character vector of metrics to include (default:
all 11 extended metrics)}
}
\value{
Named list with:
\describe{
\item{extended_combined}{Merged data.frame (long format)}
\item{mean_aggregated}{data.frame of mean metric values per Assay}
\item{mean_ranking}{data.frame with rank columns and rank_final}
\item{n_methods}{Number of unique methods}
\item{n_comparisons}{Number of unique comparisons}
\item{metrics_used}{Metrics actually used}
}
}
\description{
For each of the 11 metrics, averages across comparisons per Assay (mean),
then ranks methods in descending order (higher = better for all metrics).
Final rank = mean of the 11 individual ranks.
}
\details{
Nota: este ranking extendido puede desalinearse del ranking OpDEA canónico
(\code{bm_compute_ranking}, basado en nMCC/G_mean/pAUC). \code{Performance} es colineal
con F1 (por lo que F1 pesa doble), y \code{Accuracy}/\code{NPV} en spike-ins están
dominadas por el fondo (TN >> ) → poco poder discriminativo. Úsese como vista
complementaria, no como sustituto del ranking OpDEA.
}
