% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Pattern_Profiler_Analysis.R
\name{pattern_profiler_analysis}
\alias{pattern_profiler_analysis}
\title{Pattern Profiler Analysis}
\usage{
pattern_profiler_analysis(
  se_proc,
  DEPs_results,
  assay_name = NULL,
  filter_mode = c("any", "all", "specific"),
  alpha = 0.05,
  comparison = NULL,
  condition_order = NULL,
  aggregate = c("median", "mean"),
  c_range = 2:10,
  auto_select_c = TRUE,
  c = NULL,
  selection_method = c("xb", "consensus", "elbow"),
  min_membership = 0.25,
  seed = 42,
  seeds = c(42, 123, 456),
  output_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{se_proc}{SummarizedExperiment with intensity data}

\item{DEPs_results}{DataFrame with differential expression results (must have an 'Assay' column)}

\item{assay_name}{Name of the assay to cluster. It is also the value used to
filter \code{DEPs_results} by its \code{Assay} column, so the two must agree. With
the default \code{NULL} it is taken from \code{DEPs_results$Assay}, which records the
assay the differential abundance was computed on; supply it explicitly when
those results cover more than one assay.}

\item{filter_mode}{Filtering mode: "any" (significant in at least one
comparison), "all" (ALL features, with no significance filtering),
"specific" (significant in the comparison given by \code{comparison})}

\item{alpha}{Significance threshold (default: 0.05)}

\item{comparison}{Specific comparison (for filter_mode="specific")}

\item{condition_order}{Condition order for the profiles}

\item{aggregate}{Aggregation method: "median" or "mean"}

\item{c_range}{Range of cluster numbers to evaluate}

\item{auto_select_c}{Automatic selection of the number of clusters (default: TRUE)}

\item{c}{Fixed number of clusters (if auto_select_c=FALSE)}

\item{selection_method}{Selection method: "xb", "consensus", "elbow"}

\item{min_membership}{Minimum membership threshold for inclusion in the output}

\item{seed}{Integer. Seed for the Mfuzz clustering itself. Fuzzy c-means
starts from a random partition, so the cluster labels and the memberships
depend on it; fix it to make a run reproducible. The caller's RNG state is
restored on exit. Default 42.}

\item{seeds}{Numeric vector of seeds used when \code{auto_select_c = TRUE}: each
candidate number of clusters is fitted once per seed and the metrics are
averaged, so that the choice of \code{c} does not hang on a single partition.
Default \code{c(42, 123, 456)}.}

\item{output_file}{Path of the output parquet file. \code{NULL} by default, which
writes nothing to disk; the long-format data is returned regardless in the
\code{long_output} element of the result.}

\item{verbose}{Show progress messages}
}
\value{
List (invisible) with the clustering results: \code{optimal_c}, \code{m},
\code{conditions}, feature counts, \code{selection_metrics}, \code{cluster_counts}, the
Mfuzz \code{cl} object, the standardised \code{eset_std} and \code{long_output}, the
long-format data.frame that is written when \code{output_file} is given.
}
\description{
Complete clustering pipeline starting from a SummarizedExperiment.
Writes a parquet file in LONG format for visualisation.
}
\examples{
if (requireNamespace("Mfuzz", quietly = TRUE) &&
    requireNamespace("e1071", quietly = TRUE)) {
  data(nadia_dia)
  res <- process_proteomics(nadia_dia, verbose = FALSE)

  # auto_select_c = TRUE would cluster once per value of c_range
  pp <- pattern_profiler_analysis(
    res$se_proc,
    DEPs_results    = res$DEPs_results,
    assay_name      = "Impseqrob_min",
    filter_mode     = "any",
    condition_order = c("A", "B", "D"),
    auto_select_c   = FALSE,
    c               = 3,
    verbose         = FALSE
  )
  print(pp$cluster_counts)
  print(head(pp$long_output))
}

}
