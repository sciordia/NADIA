% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{nadia_connect}
\alias{nadia_connect}
\title{Open a connection to a \code{.nadia} file}
\usage{
nadia_connect(file, read_only = TRUE)
}
\arguments{
\item{file}{Path to a \code{.nadia} file.}

\item{read_only}{Open without write access. \code{TRUE} by default, which is also
what allows several processes to read the same file at once.}
}
\value{
A DBI connection.
}
\description{
For querying the file directly with SQL or dplyr. Most users want
\code{\link[=read_nadia]{read_nadia()}} instead; this is the way in when the file is larger than
memory or when a specific query beats loading everything.
}
\details{
The caller owns the connection and must close it with
\code{DBI::dbDisconnect(con, shutdown = TRUE)}.
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    f <- file.path(tempdir(), "query.nadia")
    write_nadia(f, nadia_dia,
                process_proteomics(nadia_dia, verbose = FALSE),
                verbose = FALSE)

    con <- nadia_connect(f)
    DBI::dbGetQuery(con, "SELECT COUNT(*) AS n FROM proteins")
    DBI::dbDisconnect(con, shutdown = TRUE)

    unlink(f)
}
}
\seealso{
\code{\link[=read_nadia]{read_nadia()}}, \code{\link[=nadia_tables]{nadia_tables()}}
}
