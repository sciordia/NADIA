% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Benchmarking_Single.R
\name{compute_opdea_metrics}
\alias{compute_opdea_metrics}
\title{Compute OpDEA metrics for all comparisons}
\usage{
compute_opdea_metrics(
  de_res,
  ev,
  alpha = 0.05,
  lfc_thr = 0,
  p_col = "adj.P.Val",
  comparisons = NULL,
  assay = NULL,
  species_df = NULL
)
}
\arguments{
\item{de_res}{Data frame with DE results}

\item{ev}{Data frame with expected values}

\item{alpha}{Significance threshold (default: 0.05)}

\item{lfc_thr}{Log fold-change threshold (default: 0)}

\item{p_col}{P-value column name (default: "adj.P.Val")}

\item{comparisons}{Comparisons to include (NULL = all)}

\item{assay}{Assay to filter (NULL = all)}

\item{species_df}{Data frame with Protein.IDs and Species columns (optional).
If NULL, de_res must already contain a Species column.}
}
\value{
Data frame with columns: Comparison, nMCC, G_mean,
pAUC_001, pAUC_005, pAUC_010
}
\description{
Calculates additional benchmark metrics based on Peng et al. 2024
(Nature Comms, OpDEA framework):
\itemize{
\item pAUC(0.01), pAUC(0.05), pAUC(0.1): Partial AUC with McClish correction
\item nMCC: Normalized MCC = (MCC + 1) / 2
\item G-mean: sqrt(Sensitivity * Specificity)
}
}
\examples{
data(nadia_dia)
de <- process_proteomics(nadia_dia, verbose = FALSE)$DEPs_results
sp <- utils::read.delim(system.file("extdata", "nadia_dia_spikein.tsv.gz",
                                    package = "NADIA"))
de$Species <- sp$PG.OrganismId[match(de$Protein.IDs, sp$PG.ProteinGroups)]
ev <- data.frame(Comparison = rep(c("B-A", "D-A"), each = 2),
                 Species = c("ECOLI", "YEAST"),
                 expected_logFC = c(1, -0.58, 2, -3.3))
compute_opdea_metrics(de, ev)
}
