% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{im_plot_ranking}
\alias{im_plot_ranking}
\title{Ranking heatmap of imputation methods}
\usage{
im_plot_ranking(metrics_df, ...)
}
\arguments{
\item{metrics_df}{data.frame from \code{im_compute_metrics()}.}

\item{...}{Additional arguments (unused).}
}
\value{
ggplot object.
}
\description{
Heatmap (geom_tile + geom_text) showing per-metric ranks and the
combined Rank_Mean. Rows = methods (ordered by Rank_Mean), columns = metrics.
}
\examples{
data(nadia_dia)
se <- im_prepare_se(nadia_dia, norm_method = "cycloess", verbose = FALSE)
metrics <- im_compute_metrics(se, methods = c("min", "zero", "knn"),
                              verbose = FALSE)
im_plot_ranking(metrics)
}
