% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{imputation_metrics}
\alias{imputation_metrics}
\title{Generate imputation quality metric plots and ranking}
\usage{
imputation_metrics(
  se,
  assay_name = NULL,
  methods = .IM_BENCH_METHODS,
  combo_methods = list(),
  condition_col = "Condition",
  na_prop = 0.2,
  seed = 42L,
  pattern = "random",
  method_args = list(),
  with_value = NA_real_,
  plots = "all",
  verbose = TRUE,
  output_dir = NULL,
  export_plots = TRUE,
  export_tables = TRUE,
  plot_width = 12,
  plot_height = 8,
  plot_dpi = 150
)
}
\arguments{
\item{se}{SummarizedExperiment (from \code{import_imp_matrices()} or pipeline).}

\item{assay_name}{Character. Assay name to use as starting point.
NULL (default) = first assay.}

\item{methods}{Character vector of individual methods to benchmark.
Default: \code{.IM_BENCH_METHODS} (16 methods). Use \code{NULL} or \code{character(0)}
to skip individual methods when only using combo_methods.}

\item{combo_methods}{Named list of combo/softHybrid configurations.
Each element: \code{list(mar_method, mnar_method, mode)}.
See \code{im_compute_metrics()} for details.}

\item{condition_col}{Character. Column in colData for combo methods.
Default \code{"Condition"}.}

\item{na_prop}{Numeric (0-1). Proportion of artificial NAs. Default 0.20.
Only used when \code{pattern = "random"}. Ignored when \code{pattern = "from_data"}
(NA proportion is derived from the data, replicating NAguideR).}

\item{seed}{Integer. Random seed. Default 42.}

\item{pattern}{Character. \code{"random"} or \code{"from_data"}. Default \code{"random"}.}

\item{method_args}{Named list of per-method argument lists.}

\item{with_value}{Constant for method \code{"with"}.}

\item{plots}{Character vector of plot names or \code{"all"} (default).
Valid: \code{"nrmse"}, \code{"sor"}, \code{"pss"}, \code{"acc_oi"}, \code{"ranking"}, \code{"metrics"}.}

\item{verbose}{Logical. Print progress. Default TRUE.}

\item{output_dir}{Character. Path to export directory. If \code{NULL} (default),
no files are exported. When set, tables (TSV) and/or plots (PNG) are
saved to this directory (created if needed).}

\item{export_plots}{Logical. Export plots as PNG when \code{output_dir} is set.
Default \code{TRUE}.}

\item{export_tables}{Logical. Export tables as TSV when \code{output_dir} is set.
Default \code{TRUE}.}

\item{plot_width}{Numeric. Width in inches for exported plots. Default \code{12}.}

\item{plot_height}{Numeric. Height in inches for exported plots. Default \code{8}.}

\item{plot_dpi}{Numeric. Resolution for exported plots. Default \code{150}.}
}
\value{
Named list of ggplot objects (or NULL for failed plots), plus
\code{metrics_table}: a data.frame from \code{im_compute_metrics()}.
}
\description{
Computes NRMSE, SOR, PSS, ACC_OI via ground-truth simulation, then
generates up to 6 diagnostic plots. Each plot is wrapped in \code{tryCatch()}
so that a failure in one does not abort all.
}
\examples{
data(nadia_dia)
se <- im_prepare_se(nadia_dia, norm_method = "cycloess", verbose = FALSE)

# The default sweeps 16 methods; a handful is enough to illustrate it
res <- imputation_metrics(se, methods = c("min", "zero", "knn"),
                          plots = "ranking", verbose = FALSE)
res$metrics_table

# Two-stage combos can be compared alongside the individual methods
res2 <- imputation_metrics(se, methods = c("min", "knn"),
  combo_methods = list(
    "Impseq+min" = list(mar_method = "Impseq", mnar_method = "min")),
  plots = "ranking", verbose = FALSE)
res2$metrics_table$Method
}
