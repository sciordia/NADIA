% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{nadia_pattern_profiler}
\alias{nadia_pattern_profiler}
\title{Rebuild the Pattern Profiler table from a \code{.nadia} file}
\usage{
nadia_pattern_profiler(db)
}
\arguments{
\item{db}{A \code{nadia_db} from \code{\link[=read_nadia]{read_nadia()}}.}
}
\value{
The \code{long_output} data frame, ready for
\code{\link[=cluster_profile_highchart_list]{cluster_profile_highchart_list()}}, or \code{NULL} if the file holds no
clustering.
}
\description{
Rebuild the Pattern Profiler table from a \code{.nadia} file
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    f <- file.path(tempdir(), "nopp.nadia")
    write_nadia(f, nadia_dia, verbose = FALSE)

    is.null(nadia_pattern_profiler(read_nadia(f)))

    unlink(f)
}
}
\seealso{
\code{\link[=read_nadia]{read_nadia()}}, \code{\link[=nadia_add_pattern_profiler]{nadia_add_pattern_profiler()}}
}
