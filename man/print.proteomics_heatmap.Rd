% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Heatmap_tidyHeatmap.R
\name{print.proteomics_heatmap}
\alias{print.proteomics_heatmap}
\title{Draw a proteomics heatmap}
\usage{
\method{print}{proteomics_heatmap}(x, ...)
}
\arguments{
\item{x}{A \code{proteomics_heatmap} object, as returned by \code{\link[=proteomics_heatmap]{proteomics_heatmap()}}.}

\item{...}{Further arguments passed to \code{ComplexHeatmap::draw()}.}
}
\value{
The drawn \code{HeatmapList}, invisibly. Called for its side effect.
}
\description{
Renders the object built by \code{\link[=proteomics_heatmap]{proteomics_heatmap()}} on the current graphics
device, adding the title stored in the object. Printing is what actually draws
the heatmap: building it does not.
}
\examples{
if (requireNamespace("ComplexHeatmap", quietly = TRUE) &&
    requireNamespace("tidyHeatmap", quietly = TRUE)) {
  data(nadia_dia)
  res <- process_proteomics(nadia_dia, verbose = FALSE)
  hm <- proteomics_heatmap(res$PCA_Input, mode = "any")

  # Drawing needs a graphics device; send it to a temporary file
  f <- tempfile(fileext = ".png")
  grDevices::png(f)
  print(hm)
  grDevices::dev.off()
  file.remove(f)
}

}
