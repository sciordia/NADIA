% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_plot_qq}
\alias{nm_plot_qq}
\title{Q-Q plot per method (NormalyzerDE style)}
\usage{
nm_plot_qq(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  which_sample = NULL,
  ...
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{which_sample}{Character. Name of the sample to inspect.
Default = first column of \code{se}.}

\item{...}{Additional arguments (unused).}
}
\value{
ggplot object.
}
\description{
Quantile-quantile plot against a normal distribution for a single sample,
one facet per normalization method. Default uses the first column (as
NormalyzerDE does).
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                            verbose = FALSE)

nm_plot_qq(se, which_sample = "A_1")

}
