% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Database_Read.R
\name{nadia_result}
\alias{nadia_result}
\title{Rebuild the processing result from a \code{.nadia} file}
\usage{
nadia_result(db)
}
\arguments{
\item{db}{A \code{nadia_db} from \code{\link[=read_nadia]{read_nadia()}}.}
}
\value{
A \code{proteomics_result} object, or an error if the file holds only the
preprocessing step.
}
\description{
Returns a \code{proteomics_result} with everything \code{\link[=process_proteomics]{process_proteomics()}} returns,
the \code{SummarizedExperiment} included, so that every plotting function in the
package works from the file directly:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{db  <- read_nadia("experiment.nadia")
res <- nadia_result(db)
volcano_highchart_list(res$DEPs_results)
pca_highchart_list(res$PCA_Input)
}\if{html}{\out{</div>}}
}
\examples{
if (requireNamespace("duckdb", quietly = TRUE)) {
    data(nadia_dia)
    res <- process_proteomics(nadia_dia, verbose = FALSE)
    f <- file.path(tempdir(), "result.nadia")
    write_nadia(f, nadia_dia, res, verbose = FALSE)

    back <- nadia_result(read_nadia(f))
    identical(back$DEPs_results, res$DEPs_results)
    class(volcano_highchart_list(back$DEPs_results)[[1]])

    unlink(f)
}
}
\seealso{
\code{\link[=read_nadia]{read_nadia()}}, \code{\link[=nadia_preprocessing]{nadia_preprocessing()}}
}
