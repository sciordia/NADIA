% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{.im_introduce_na}
\alias{.im_introduce_na}
\title{Introduce artificial NAs into a complete-case matrix}
\usage{
.im_introduce_na(
  mat,
  na_prop = 0.2,
  seed = 42L,
  pattern = "random",
  ref_mat = NULL
)
}
\arguments{
\item{mat}{Numeric matrix (proteins x samples), no NAs (ground truth)}

\item{na_prop}{Numeric (0-1). Proportion of values to set as NA.
Default 0.20. Only used when \code{pattern = "random"};
ignored when \code{pattern = "from_data"}.}

\item{seed}{Integer. Random seed. Default 42.}

\item{pattern}{Character: \code{"random"} for uniform random NAs, or
\code{"from_data"} to replicate NAguideR's simulation strategy using
the actual NA structure from \code{ref_mat}.}

\item{ref_mat}{Numeric matrix. Reference matrix with real NAs, used when
\code{pattern = "from_data"}. Ignored otherwise.}
}
\value{
Named list:
\item{mat_with_na}{Matrix with artificial NAs introduced}
\item{na_mask}{Logical matrix (TRUE = artificially set to NA)}
\item{true_mat}{Original complete matrix (unchanged)}
\item{summary}{List with n_total, n_na, na_rate, rows_affected,
row_ratio (from_data only)}
}
\description{
Takes a matrix with no NAs (ground truth) and introduces NAs at random
or mimicking the NA pattern from a reference matrix.
}
\details{
When \code{pattern = "from_data"}, replicates the NAguideR strategy
(Wang et al., DOI:10.1093/nar/gkz903): the proportion of affected rows
and the per-column NA distribution are derived entirely from \code{ref_mat},
so \code{na_prop} is ignored.
}
\keyword{internal}
