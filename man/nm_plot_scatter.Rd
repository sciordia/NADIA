% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_plot_scatter}
\alias{nm_plot_scatter}
\title{Sample-vs-sample scatter plot per method (NormalyzerDE style)}
\usage{
nm_plot_scatter(
  se,
  assay_names = NULL,
  condition_col = "Condition",
  sample1 = NULL,
  sample2 = NULL,
  ...
)
}
\arguments{
\item{se}{SummarizedExperiment from \code{import_norm_matrices()}.}

\item{assay_names}{Character vector of assay names to include. NULL = all.}

\item{condition_col}{Column in colData with condition labels.}

\item{sample1}{Character. Name of the first sample (x-axis).
Default = first column of \code{se}.}

\item{sample2}{Character. Name of the second sample (y-axis).
Default = second column of \code{se}.}

\item{...}{Additional arguments (unused).}
}
\value{
ggplot object.
}
\description{
Plots log2-intensity of one sample against another, one point per protein,
for each normalization method. A linear fit and the adjusted R^2 are overlaid.
Default comparison uses the first two columns of the SE (as NormalyzerDE does).
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                            verbose = FALSE)

nm_plot_scatter(se, sample1 = "A_1", sample2 = "A_2")

}
