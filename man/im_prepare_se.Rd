% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Imputation_Metrics.R
\name{im_prepare_se}
\alias{im_prepare_se}
\title{Build a SummarizedExperiment for imputation benchmarking}
\usage{
im_prepare_se(
  preprocessing,
  norm_method = NULL,
  pc1_rank = NULL,
  min_reps = NULL,
  min_groups = 1,
  covariate_df = NULL,
  norm_method_args = list(),
  include_log2 = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{preprocessing}{\code{proteomics_data} list from \code{preprocess_spectronaut()} or \code{preprocess_tmt()}.}

\item{norm_method}{Character scalar. Normalization method name (e.g.
\code{"cycloess"}). If provided, used directly. Default \code{NULL}.}

\item{pc1_rank}{data.frame from \code{nm_rank_pc1()} or
\code{normalization_metrics()$pc1_rank}. The first row's \code{Method} column is
used as winner. Ignored if \code{norm_method} is provided. Default \code{NULL}.}

\item{min_reps}{Minimum replicates with non-NA values per group for protein
filtering. If NULL, auto-computed as half the smallest group. Default \code{NULL}.}

\item{min_groups}{Minimum groups meeting \code{min_reps} (default: 1).}

\item{covariate_df}{Optional covariate data.frame for paired designs
(must contain a \code{Column} column). Default \code{NULL}.}

\item{norm_method_args}{Named list of per-method arguments for normalization.
Default \code{list()}.}

\item{include_log2}{Logical. Include the baseline \code{"log2"} assay in the
returned SE. Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
SummarizedExperiment with assays \code{"log2"} (optional) + winner method.
}
\description{
Convenience function that creates a SE from a \code{proteomics_data} object and
applies the winning normalization method (from normalization benchmarking).
The result is ready for \code{imputation_metrics()}.
}
\details{
Internally calls \code{nm_prepare_se()} to build the baseline SE with assay
\code{"log2"}, then applies the chosen normalization via
\code{.nm_dispatch_normalization()}.
}
\examples{
\dontrun{
source("R/Imputation_Metrics.R")

# ---- Option A: known method ----
se_imp <- im_prepare_se(preprocessing, norm_method = "cycloess")

# ---- Option B: auto-pick from pc1_rank ----
# (after running normalization_metrics() in Normalization_Metrics.R)
se_imp <- im_prepare_se(preprocessing, pc1_rank = nm_res$pc1_rank)

SummarizedExperiment::assayNames(se_imp)  # "log2", "cycloess"
res <- imputation_metrics(se_imp, assay_name = "cycloess")
}
}
