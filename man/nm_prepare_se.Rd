% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_prepare_se}
\alias{nm_prepare_se}
\title{Prepare a SummarizedExperiment from a proteomics_data object}
\usage{
nm_prepare_se(
  preprocessing,
  min_reps = NULL,
  min_groups = 1,
  covariate_df = NULL,
  verbose = TRUE
)
}
\arguments{
\item{preprocessing}{\code{proteomics_data} list from \code{preprocess_spectronaut()} or \code{preprocess_tmt()}.}

\item{min_reps}{Minimum replicates with non-NA values per group for protein
filtering. If NULL, auto-computed as half the smallest group. Default \code{NULL}.}

\item{min_groups}{Minimum groups meeting \code{min_reps} (default: 1).}

\item{covariate_df}{Optional covariate data.frame for paired designs
(must contain a \code{Column} column). Default \code{NULL}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
SummarizedExperiment with assays \code{"raw"} and \code{"log2"}.
}
\description{
Convenience wrapper that extracts metadata and protein data from a
\code{proteomics_data} object (output of \code{preprocess_spectronaut()} or
\code{preprocess_tmt()}), performs zero-to-NA conversion, protein filtering,
and returns a SE with assays \code{"raw"} and \code{"log2"} — ready for
\code{nm_run_normalizations()} or \code{normalization_metrics(..., methods = "all")}.
}
\details{
Internally calls \code{normalize_proteomics()} with \code{norm_method = "log2"}
(no additional normalization).
}
\examples{
\dontrun{
source("R/Normalization_Metrics.R")
se <- nm_prepare_se(preprocessing, min_reps = 3)
plots <- normalization_metrics(se, methods = "all")
}
}
