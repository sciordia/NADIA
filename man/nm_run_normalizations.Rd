% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Normalization_Metrics.R
\name{nm_run_normalizations}
\alias{nm_run_normalizations}
\title{Run multiple normalization methods from a baseline assay}
\usage{
nm_run_normalizations(
  se,
  assay_name = "log2",
  methods = "all",
  method_args = list(),
  include_baseline = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{se}{SummarizedExperiment with at least one log2-scale assay.}

\item{assay_name}{Name of the baseline assay to normalize from.
Default \code{"log2"}.}

\item{methods}{Character vector of method names, or \code{"all"} for all 12
benchmark methods. Default \code{"all"}.}

\item{method_args}{Named list of per-method arguments. E.g.
\code{list(cycloess = list(method = "fast", span = 0.8))}.}

\item{include_baseline}{Logical. Include the baseline assay in the output SE.
Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
SummarizedExperiment with one assay per successfully normalized method
(plus baseline if \code{include_baseline = TRUE}).
}
\description{
Takes a SummarizedExperiment with a log2-scale assay and applies each
requested normalization method, returning a new SE with one assay per method.
}
\examples{
data(nadia_dia)
se <- nm_prepare_se(nadia_dia, verbose = FALSE)
se_bench <- nm_run_normalizations(se, methods = c("cycloess", "quantile", "MAD"),
                                  verbose = FALSE)
SummarizedExperiment::assayNames(se_bench)

}
