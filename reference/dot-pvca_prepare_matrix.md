# Extract and prepare the abundance matrix, handling NAs

Extract and prepare the abundance matrix, handling NAs

## Usage

``` r
.pvca_prepare_matrix(
  se,
  assay_name,
  na_action = "complete",
  fill_value = -1,
  verbose = TRUE
)
```
