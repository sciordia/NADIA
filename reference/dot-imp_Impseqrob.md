# Impseqrob: Robust sequential imputation (rrcovNA)

Impseqrob: Robust sequential imputation (rrcovNA)

## Usage

``` r
.imp_Impseqrob(x, args = list())
```

## Arguments

- args:

  list with optional `alpha` (default 0.9)
