# knn: k-nearest neighbors imputation (impute)

knn: k-nearest neighbors imputation (impute)

## Usage

``` r
.imp_knn(x, args = list())
```

## Arguments

- args:

  list with optional `k` (default 10), `rowmax` (default 0.99), `colmax`
  (default 0.99).
