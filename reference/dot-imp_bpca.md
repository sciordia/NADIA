# bpca: Bayesian PCA imputation (pcaMethods)

bpca: Bayesian PCA imputation (pcaMethods)

## Usage

``` r
.imp_bpca(x, args = list())
```

## Arguments

- args:

  list with optional `nPcs` (default ncol-1), `maxSteps` (default 100)
