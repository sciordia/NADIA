# Configure species colors

Configure species colors

## Usage

``` r
.configure_species_colors(species, colors = NULL)
```

## Arguments

- species:

  Character vector of unique species names

- colors:

  Named vector of custom colors (optional)

## Value

Named vector of colors per species
