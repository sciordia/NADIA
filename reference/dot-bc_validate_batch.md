# Validate batch column in colData

Validate batch column in colData

## Usage

``` r
.bc_validate_batch(se, batch_column)
```

## Arguments

- se:

  SummarizedExperiment

- batch_column:

  Character. Column name in colData
