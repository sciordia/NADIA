# Create an ExpressionSet for Mfuzz

Create an ExpressionSet for Mfuzz

## Usage

``` r
create_expression_set(mat, feature_info = NULL)
```

## Arguments

- mat:

  Expression matrix (features x conditions)

- feature_info:

  DataFrame with feature information (optional)

## Value

ExpressionSet object
