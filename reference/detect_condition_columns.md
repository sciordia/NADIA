# Detect the condition columns (z-scores)

Detect the condition columns (z-scores)

## Usage

``` r
detect_condition_columns(data)
```

## Arguments

- data:

  Pattern Profiler DataFrame

## Value

Vector with the names of the condition columns
